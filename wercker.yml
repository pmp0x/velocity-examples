box: danieldent/meteor:latest
no-response-timeout: 12
services:
    - wercker/mongodb
# build:
#     steps:
#         # Execute the bundle install step, a step provided by wercker
#         #- git clone https://github.com/meteor/meteor.git $WERCKER_ROOT/meteor
#         - script:
#             name: echo python information
#             code: |
#                 echo "ls $(ls -la)"
#                 echo "dir $(pwd)"

build:
  # The steps that will be executed on build
  steps:
    # A step that executes `npm install` command
    - script:
        name: NPM version
        code: echo "npm version $(npm --version)"

    - script:
        name: Testing
        code:  JASMINE_BROWSER=PhantomJS cd leaderboard-jasmine && meteor --test
